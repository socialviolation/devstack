version: 1

service:
  name: telemetry-bad

runtime:
  workDir: .
  run:
    command: ../../scripts/run-placeholder.sh telemetry-bad
  restart:
    strategy: process

telemetry:
  traces:
    expected: true
  logs:
    expected: true
  serviceName: telemetry-bad
