version: 1

service:
  name: crashy

runtime:
  workDir: .
  run:
    command: python3 app.py
  restart:
    strategy: process

telemetry:
  traces:
    expected: false
  logs:
    expected: true
  serviceName: crashy
