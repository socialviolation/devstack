version: 1

service:
  name: telemetry-good

runtime:
  workDir: .
  run:
    command: python3 app.py
  restart:
    strategy: process

telemetry:
  traces:
    expected: true
  logs:
    expected: true
  serviceName: telemetry-good
