version: 1

service:
  name: worker

runtime:
  workDir: .
  run:
    command: python3 app.py
  restart:
    strategy: process

env:
  files:
    - .envrc

ports:
  http: 9090

telemetry:
  traces:
    expected: false
  logs:
    expected: true
  serviceName: worker
